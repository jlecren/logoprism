#ifndef __LOGOPRISM_UTILS_SIGNALS_HPP__
#define __LOGOPRISM_UTILS_SIGNALS_HPP__

namespace logoprism {
  namespace utils {

    namespace signals {

      void kill();
      bool is_killed();
      void install();

    }

  }
}

#endif // ifndef __LOGOPRISM_UTILS_SIGNALS_HPP__
